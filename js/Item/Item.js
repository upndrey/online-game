let Item = function (name, priceBuy, priceSell, width, height, stats) {
    this.name = name;
    this.priceBuy = priceBuy;
    this.priceSell = priceSell;
    this.width = width;
    this.height = height;
    this.stats = stats;
};

export default Item;